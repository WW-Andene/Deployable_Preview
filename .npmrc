## Prevent npm install from rewriting package.json when
## versions already satisfy the declared ranges.
save=false

## Prefer exact versions when adding new dependencies so that
## package.json stays deterministic across environments.
save-exact=true
